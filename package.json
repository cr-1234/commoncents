{
  "name": "commoncents",
  "version": "1.0.0",
  "description": "CommonCents UI with unified border radius",
  "scripts": {
    "dev": "live-server --port=8080 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
